@model IEnumerable<AlunoViewModel>

<br />
<br />
<div class="container p-4 border">
	<div class="row pb-2">
		<div class="col">
			<h1 class="text-primary">Lista de Alunos</h1>
		</div>
		<div class="col text-end pt-1">
			<a asp-action="CreateAluno" class="btn btn-outline-primary"><i class="fas fa-plus"></i> Adicionar aluno </a>
		</div>
	</div>
	<table class="table">
		<thead>
			<tr>
				<th>
					Nome
				</th>
				<th>
					Usuário
				</th>
				<th>
					Senha
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@item.Nome
					</td>
					<td>
						@item.Usuario
					</td>
					<td>
						@item.Senha
					</td>
					<td>
						<a asp-action="UpdateAluno" class="btn btn-success" asp-route-id="@item.Id">
							<i class="fas fa-edit"></i>
						</a>
						<a asp-action="DeleteAluno" class="btn btn-danger" asp-route-id="@item.Id">
							<i class="fas fa-trash"></i>
						</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>